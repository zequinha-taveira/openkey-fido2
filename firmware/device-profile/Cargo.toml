[package]
name = "device-profile"
version = "0.1.1"
edition = "2021"
description = "Device profile and capability discovery for FIDO2 authenticators"
license = "MIT"

[dependencies]
board-generic = { path = "../board-generic" }
ctap2 = { path = "../../protocol/ctap2" }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
