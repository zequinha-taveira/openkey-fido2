[package]
name = "transport"
version = "0.1.0"
edition = "2021"
description = "Transport layer for FIDO2 authenticator (USB-HID, CCID, NFC, BLE)"
license = "MIT"

[dependencies]
log = "0.4"
thiserror = "2.0"

[dependencies.embedded-hal]
version = "1.0"
optional = true

[features]
default = []
embedded = ["dep:embedded-hal"]
