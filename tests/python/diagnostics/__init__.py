"""Fluxo de diagnóstico openkey-fido2.

Ciclo completo: executar catálogo de falhas → capturar exceções → atribuir
a camada responsável → apontar escopo de correção → travar baseline no fio
(golden master) para regressão automática.
"""
