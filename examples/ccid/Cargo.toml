[package]
name = "ccid-example"
version = "0.1.0"
edition = "2021"
description = "FIDO2 authenticator with CCID interface example"
license = "MIT"

[dependencies]
authenticator = { path = "../../firmware/authenticator" }
board-generic = { path = "../../firmware/board-generic" }
log = "0.4"
env_logger = "0.11"